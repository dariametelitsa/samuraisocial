import styled from "styled-components";

const Input = styled.input `
    padding: 4px 10px;
    border-radius: 6px;
`;

const Textarea = styled.textarea `
    padding: 4px 10px;
    border-radius: 6px;
    resize: none;
    width: 100%;
`;

export const Style = {
    Input,
    Textarea
}